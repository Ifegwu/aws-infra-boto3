[project]
name = "aws-infra-boto3"
version = "0.1.0"
description = "Simple EC2 deployment script using boto3"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3",
    "botocore>=1.42.89",
]
